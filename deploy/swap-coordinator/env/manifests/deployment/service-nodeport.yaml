apiVersion: v1
kind: Service
metadata:
  name: swap-coordinator-nodeport
  namespace: swap
  labels:
    app: swap-coordinator
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30080
    protocol: TCP
    name: http
  selector:
    app: swap-coordinator
